export type NavigationLink = {
  href: string;
  label: string;
};

export type CareerRole = {
  title: string;
  department: string;
  location: string;
  type: string;
  summary: string;
};

export type ResearchArea = {
  id: string;
  title: string;
  desc: string;
};
